type ferjeData @model {
  id: ID!
  topic: AWSJSON
  config:AWSJSON
  GMT : AWSTimestamp
  ID:Int
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}


type Subscription {
  onUpdateById(id: ID!): ferjeData
    @aws_subscribe(mutations: ["updateFerjeData"])
}
